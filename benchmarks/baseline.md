# Performance baseline

> **STATUS: PENDING.** Populate from the FIRST run of `benchmark.yml` on
> GitHub Actions (ubuntu-latest), NOT from a local machine — the regression
> comparison runs on ubuntu-latest, so the baseline must be measured in the
> same environment to be meaningful. See `benchmarks/README.md` for the procedure.

- **Date:** (fill on first run)
- **Commit:** (fill: git short SHA of the benchmarked commit)
- **Runner:** GitHub Actions ubuntu-latest
- **BenchmarkDotNet:** 0.15.8

<!-- Replace this block with the *-report-github.md tables from the first run. -->
